# Scripts

This directory contains both the new phase-oriented script layout and a compatibility layer.

## Notes

- `scripts/legacy/` preserves the original template scripts that were used as seed tooling.
- phase-specific implementations live under `scripts/phases/`.
- root-level scripts provide orchestration, session lifecycle, and graph operations.
