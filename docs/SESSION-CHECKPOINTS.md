# Session Checkpoints

Store session checkpoints in:

- `sessions/checkpoints/` for project sessions
- `editorial/checkpoints/` for broader editorial state if needed

Each checkpoint should include:

- objective
- inputs
- prompts used
- files changed
- graph impact
- next session
