# CHECKPOINT — v2 update

The template bundle was updated with:
- migrated original seed scripts into the new phase-oriented structure
- legacy compatibility copies
- `init.sh` v2
- missing starter templates required by initialization

A validation run against an `AI Systems Engineering` example project completed successfully.
See `docs/INIT-VALIDATION.md`.
